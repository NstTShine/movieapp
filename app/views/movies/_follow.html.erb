<%= form_for(current_user.active_likes.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :movie_id, @movie_support.movie.id %></div>
  <div class="btn-actions fl-left">
    <%= f.submit "Likes", class: "glyphicon glyphicon-thumbs-up" %>
  </div>
<% end %>
